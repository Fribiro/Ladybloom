import React from 'react'
import ProtectedHeader from './ProtectedHeader.style';

const Packages = () => {
    return (
      <div>
        <ProtectedHeader
          loantitle={"Packages"}
          loanHeadLink={"Packages"}
        ></ProtectedHeader>
      </div>
    );
}

export default Packages
